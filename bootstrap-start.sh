#To install symbol-bootstrap https://github.com/nemtech/symbol-bootstrap

symbol-bootstrap start -p bootstrap -a demo -t target/bootstrap -c bootstrap-preset.yml --noPassword --healthCheck --timeout 120000 $1 $2 $3
